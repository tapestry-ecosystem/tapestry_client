[pytest]
testpaths = unit_tests
pythonpath = src .
asyncio_mode = auto
addopts = --strict-config --strict-markers --unit-isolation
markers =
    unit: isolated transport, schema, and webhook rules
    isolated_unit: proven unit boundary without application or I/O dependencies
